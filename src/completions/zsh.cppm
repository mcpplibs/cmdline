module;

export module mcpplibs.cmdline:completions.zsh;

import std;
import :completions;

namespace mcpplibs::cmdline::completions::zsh {

export void generate(const completions::Command& /*cmd*/, std::ostream& /*out*/) {
    // TODO
}

}; // namespace mcpplibs::cmdline::completions::zsh
